exports=function(e) {
   var z, db
      ,  pc=require('pops/pops.core')
   	,	pdbm=require('pops/db/pops.db.mongo')
   ;
	LOG('_'.Repeat(60) + '\n pops/db/mongo:construct\n' + '_'.Repeat(60));

	z=new pdbm.Dict();

	db=new pdbm.db({
			name: 'test'
		,	host: 'localhost'
		,	port: 60
	});

};